import AppController from "./app/controller.js";
import { attachHandlers } from "./ui/handlers.js";
import "./styles.css";

document.addEventListener("DOMContentLoaded", () => {
  // TODO: AppController.init();
  // TODO: attachHandlers(rootElement);
});
